import React from 'react';

const DeviceDetails = (props) => {
    return (
        <div>
            <p>Informatuon about Device: {props.price}</p>
        </div>
    );
};

export default DeviceDetails;